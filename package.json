{
  "name": "watchjunto",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "dev": "concurrently -n server,client -c yellow,cyan \"npm run dev -w apps/server\" \"npm run dev -w apps/client\"",
    "build": "npm run build -w apps/server && npm run build -w apps/client",
    "start": "npm run start -w apps/server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
